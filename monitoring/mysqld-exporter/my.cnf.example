# cp my.cnf.example my.cnf 후 값 채우기 (my.cnf 는 gitignore)
# RDS 엔드포인트 + 아래 sql/rds-monitor-user.sql 로 만든 read-only 모니터 유저
[client]
host = airdnd-db.c3u2kcgokcam.ap-northeast-2.rds.amazonaws.com
port = 3306
user = exporter
password = REPLACE_WITH_MONITOR_PASSWORD
